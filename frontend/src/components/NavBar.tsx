import React from 'react';
import './NavBar.css';

const NavBar: React.FC = () => {
  return (
    <nav className="nav-bar">
      <div className="nav-logo">Oneβöard</div>
    </nav>
  );
};

export default NavBar;
